// Import and re-export the ToolboxInterface from salesperson as MarketingSalespersonToolbox
// This provides the same interface as the salesperson toolbox
import ToolboxInterface from '../salesperson/ToolboxInterface';

// Re-export with the new name - this is a direct copy/reference to the same component
export default ToolboxInterface;
